apiVersion: v1
kind: Service
metadata:
  name: {{ include "amq-streams-template.fullname" . }}
  labels:
    {{- include "amq-streams-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amq-streams-template.selectorLabels" . | nindent 4 }}
