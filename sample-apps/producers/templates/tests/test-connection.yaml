apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "producers.fullname" . }}-test-connection"
  labels:
    {{- include "producers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "producers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
