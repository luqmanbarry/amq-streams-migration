apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amq-streams-template.fullname" . }}-test-connection"
  labels:
    {{- include "amq-streams-template.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amq-streams-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
