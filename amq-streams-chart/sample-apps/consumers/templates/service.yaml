apiVersion: v1
kind: Service
metadata:
  name: {{ include "consumers.fullname" . }}
  labels:
    {{- include "consumers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "consumers.selectorLabels" . | nindent 4 }}
