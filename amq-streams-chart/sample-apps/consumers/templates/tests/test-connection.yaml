apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "consumers.fullname" . }}-test-connection"
  labels:
    {{- include "consumers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "consumers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
